import {
  TextureLoader,
  Vector2
} from "./chunk-DPMNFJ3R.js";

// node_modules/three/examples/jsm/shaders/DitherShader.js
var DitherShader = {
  name: "DitherShader",
  uniforms: {
    "tDiffuse": { value: null },
    "opacity": { value: 1 },
    "bayer": { value: new TextureLoader().load("public/bayer.png") },
    "viewport_size": { value: new Vector2(canvas.width, canvas.height) }
  },
  vertexShader: (
    /* glsl */
    `

		varying vec2 vUv;

		void main() {

			vUv = uv;
			gl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );

		}`
  ),
  fragmentShader: (
    /* glsl */
    `

		uniform float opacity;

		uniform sampler2D tDiffuse;
        uniform sampler2D bayer;
        uniform vec2 viewport_size;

		varying vec2 vUv;

		void main() {
            
			vec4 texel = texture2D( tDiffuse, vUv );

            vec4 out_color = step(texture2D(tDiffuse, vUv/8.).r, texture(bayer,vUv/viewport_size.xy));
			gl_FragColor = out_color;
		}`
  )
};
export {
  DitherShader
};
//# sourceMappingURL=three_addons_shaders_DitherShader__js.js.map
